export const WHITELIST = [
  'swop.fi',
  'testnet.waves.exchange',
  'vires.finance',
  'waves-dapp.com',
  'waves-ide.com',
  'waves.exchange',
  'wavesducks.com',
  'web.keeper-wallet.app',
  'swap.keeper-wallet.app',
  'app.power.tech',
  'wx.network',
];

export const CUSTOMLIST = ['docs.waves.tech', 'waves.tech'];

export const DEFAULT_PASSWORD = 'default-password';

export const DEFAULT_ANIMATION_DELAY = 800;
export const DEFAULT_PAGE_LOAD_DELAY = 800;

export const DEFAULT_MINER_SEED = 'waves private node seed with waves tokens';
export const ISSUER_SEED =
  'issuer account that will receive tokens from generator';
export const USER_1_SEED = 'user number one that used in test scenarios';
export const USER_2_SEED = 'user number two that used in test scenarios';
