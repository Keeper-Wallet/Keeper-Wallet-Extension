@import '../../../styles/colors.styl';

.error
    background: $error400;
    color: $white;
    display: block;
    height: 32px;
    padding: 0 24px;
    line-height: 32px;
    position: absolute;
    bottom: 14px;
    border-radius: 2px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 14px;
