@import '../../../styles/colors.styl';

.input {
  position: relative;
  width: 100%;
  height: 40px;
  border-radius: 4px;
  border: solid 1px $basic100;
  border-color: $basic100;
  background-color: $white;

  &.error {
    background-color: rose;
    border-color: red;
  }
}

