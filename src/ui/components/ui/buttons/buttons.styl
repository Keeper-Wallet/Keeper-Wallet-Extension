@import '../../../styles/colors.styl';

:global .buttons-wrapper
  display: flex;
  align-items: center;
  justify-content: space-between;

.button
  width: 236px;
  height: 40px;
  display: block;
  border-radius: 4px;
  font-family: Roboto-Medium;
  font-size: 14px;
  text-align: center;
  white-space: nowrap;
  cursor: pointer;

  &.default
    background: $white;
    border: 1px solid $basic200;

  &:focus
    outline: none;

  &.disabled,
  &:disabled,
  &[disabled]
    cursor: default;
    background-color: $disabled200;

  &.submit
    background-color: $submit400;
    color: $white;

    &.disabled
      background-color: $submit200;
      cursor: default;

    &:hover
      background: $submit300;

  &.transparent
    background-color: transparent;

    &.disabled
      background-color: transparent;

    &:hover
      background: transparent;

  &.warning
    background-color: $error400;
    color: $white;

  &.interface
    background-color: $white;
    height: 32px;
    line-height: 32px;
    padding: 0 14px 0 40px;
    box-shadow: 0 2px 4px 0 #9ba6b266;
    font-family: Roboto;
    font-weight: 400;
    width: auto;
    margin: 0;
    border: 0.5px solid trasparent;

    &:not(:last-child)
      margin-right: 14px;

    &.disabled,
    &:disabled,
    &[disabled]
      background-color: transparent;
      border: 0.5px solid $basic200;
      box-shadow: none;
