@import '../../../styles/colors.styl';

.conditions {
  width: 100%;
  height: 100%;
  position: relative;
}

.title {
  text-transform: uppercase;
  padding-bottom: 10px;
}

.contentWrapper {
  margin: 10px;
  padding-top: 15px;
  height: 440px;
}

.attention {
  margin-top: 10px;
  margin-bottom: 20px;
}

.mark {
  margin-top: 10px;
}

