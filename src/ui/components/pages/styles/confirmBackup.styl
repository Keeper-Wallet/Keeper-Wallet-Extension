@import '../../../styles/colors.styl'

.content {
  display: block;
  position: relative;
  margin: 84px 0;

  h2 {
    width: 240px;
    margin: 0 60px 20px;
  }
}

.readSeed {
  width: calc(100% - 48px);
  height: 164px;
  margin: 0 24px 24px;
  text-align: center;
}

.writeSeed {
  width: 100%;
  height: 164px;
  padding: 26px;
  text-align: center;
}
