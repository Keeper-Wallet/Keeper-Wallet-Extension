@import '../../../styles/colors.styl';

.content
  display: block;
  position: relative;
  margin: 84px 60px 0;
  height: calc(100% - 180px);

  .footer
    position: absolute;
    bottom: 0;
    width: 100%;

    button
      margin-bottom: 44px;

.greyLine
  margin: 0 -64px 40px;
