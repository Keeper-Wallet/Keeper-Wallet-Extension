.newAccountName
  width: 100%;
  height: 100%;

.content
  display: block;
  position: relative;
  margin: 84px 60px;
  height: calc(100% - 180px);

  button {
    position: absolute;
    bottom: 0;
  }

