.newAccountName
  width: 100%;
  height: 100%;

.content
  display: block;
  position: relative;
  width: 240px;
  margin: 40px 60px;
  height: calc(100% - 180px);

  button {
    position: absolute;
    bottom: 0;
  }

